Driver question (topic={{CONTEXT_TOPIC}}, urgent={{URGENT}}):

> {{QUESTION}}

Return a conversational answer in **≤3 complete sentences**. The race
engineer will relay your answer on team radio verbatim — so write it the
way the driver should hear it (F1 radio voice: terse, numerical, second
person).

Use MCP data tools as needed. If the answer requires speculation, say so
plainly ("I don't have data on that") rather than guessing.

Auto-load `.agents/skills/responder/SKILL.md` for the canonical Q&A
heuristics if you have it.
